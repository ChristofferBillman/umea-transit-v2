import css from './ExampleComponent.module.css'

export function ExampleComponent() {
	return (
		<div className={css.container}>
			<p className={css.text}> ⚛️React + 🟦TypeScript + ⚡Vite + 🎨CSS modules </p>
		</div>
	)
}
